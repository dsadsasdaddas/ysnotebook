generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Document {
  id        String   @id @default(cuid())
  title     String
  content   String?  // JSON stringified DocBlock[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
